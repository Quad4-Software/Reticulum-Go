name: Cross-Reference Tests (Python)

on:
  push:
    branches:
      - master
      - dev
  pull_request:
    branches:
      - master
      - dev
  workflow_dispatch:

permissions:
  contents: read

env:
  CI_GO_VERSION: "1.25.5"

jobs:
  crossref:
    name: Test against Python Reticulum
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Reticulum-Go
        run: sh scripts/ci/checkout.sh 1

      - name: Set up Go
        run: sh scripts/ci/setup-go.sh "${{ env.CI_GO_VERSION }}"

      - name: Python venv and dependencies
        run: sh scripts/ci/setup-venv-pip.sh pycryptodome

      - name: Generate test vectors
        run: |
          . .venv/bin/activate
          bash tests/crossref/run_crossref.sh generate

      - name: Run crossref tests
        run: |
          . .venv/bin/activate
          bash tests/crossref/run_crossref.sh test
